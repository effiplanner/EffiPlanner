services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: effiplanner
    ports:
      - "5432:5432"
    volumes:
      - effiplanner_pg:/var/lib/postgresql/data

volumes:
  effiplanner_pg:
